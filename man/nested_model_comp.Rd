% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested_model_comp.r
\name{nested_model_comp}
\alias{nested_model_comp}
\title{Nested model comparisons for conjoint attribute heterogeneity}
\usage{
nested_model_comp(data, formula, by, id = NULL, wts = NULL)
}
\arguments{
\item{data}{A data frame containing all variables referenced in \code{formula}
and \code{by}.}

\item{formula}{A two-sided formula of the form \code{outcome ~ attr1 + attr2 + ...}. The left-hand side is the outcome variable; the right-hand side
lists the conjoint attributes to test.}

\item{by}{A character string naming the subgroup variable in \code{data}. Each
attribute is tested for heterogeneity across the levels of this variable.}

\item{id}{A one-sided formula identifying the clustering unit (e.g.
\code{~respondent_id}). When supplied without \code{wts}, a cluster-robust Wald
test is used. When supplied together with \code{wts}, it is passed to
\code{\link[survey:svydesign]{survey::svydesign()}} as the \code{ids} argument. Defaults to \code{NULL}.}

\item{wts}{A one-sided formula identifying the survey-weight variable (e.g.
\code{~weight}). When supplied, models are fitted via \code{\link[survey:svyglm]{survey::svyglm()}} and
compared with a Rao-Scott LR-test. Defaults to \code{NULL}.}
}
\value{
A named list with one element per attribute on the right-hand side
of \code{formula}. Each element is the test object returned by the underlying
comparison function:
\itemize{
\item an \code{anova.svyglm} object when \code{wts} is supplied;
\item a \code{waldtest} object when only \code{id} is supplied;
\item an \code{anova} object when neither is supplied.
}
}
\description{
Tests whether the effect of each attribute in a conjoint experiment varies
across subgroups via nested model comparison, similar to what
Leeper et al. (2020) recommend. For each attribute, a
restricted model (attribute only) is compared against an unrestricted model
(attribute × subgroup interaction). The test used depends on the arguments
supplied:
}
\details{
\itemize{
\item \strong{Weights} (\code{wts}): fits both models via \code{\link[survey:svyglm]{survey::svyglm()}} and
compares them with a Rao-Scott likelihood-ratio test
(\code{\link[survey:anova.svyglm]{survey::anova.svyglm()}}).
\item \strong{Cluster ID, no weights} (\code{id} only): fits both models via
\code{\link[stats:lm]{stats::lm()}} and compares them with a cluster-robust Wald test
(\code{\link[lmtest:waldtest]{lmtest::waldtest()}}) using \code{\link[sandwich:vcovCL]{sandwich::vcovCL()}} for the
variance–covariance matrix.
\item \strong{Neither}: fits both models via \code{\link[stats:lm]{stats::lm()}} and compares them
with a standard F-test (\code{\link[stats:anova]{stats::anova()}}). A warning is issued because
conjoint data typically has a clustering structure.
}
}
\examples{

library(conjest)
data("trust")

# Cluster-robust Wald test (typical conjoint use case)
nested_model_comp(
  data    = trust,
  formula = selected ~ group + age + class,
  by      = "resp_sex",
  id      = ~uuid
)

# Rao-Scott LR-test with survey weights
nested_model_comp(
  data    = trust,
  formula = selected ~ group + age + class,
  by      = "resp_sex",
  id      = ~uuid,
  wts     = ~weight
)

}
\references{
Leeper, T. J., Hobolt, S. B., and Tilley, J. (2020). Measuring
Subgroup Preferences in Conjoint Experiments. \emph{Political Analysis},
28(2), 207--221. \doi{10.1017/pan.2019.30}
}
\seealso{
\code{\link[survey:svyglm]{survey::svyglm()}}, \code{\link[lmtest:waldtest]{lmtest::waldtest()}}, \code{\link[sandwich:vcovCL]{sandwich::vcovCL()}},
\code{\link[stats:anova]{stats::anova()}}
}
